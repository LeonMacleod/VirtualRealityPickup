using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HandPositionMultiplier : MonoBehaviour
{

    public GameObject RightHand;

    // Start is called before the first frame update
    void Start()
    {
        
        

    }

    // Update is called once per frame
    void Update()
    {
        //Vector3 multipliedVector = this.transform.position * 2.5f;
        //RightHand.transform.position = multipliedVector;
    }
}
